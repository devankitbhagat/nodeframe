'use strict'

class joinRoomAction {

    async executeAction(clientSocketId) {
        let result = {}

        console.log(this.param1);
        return result;
    }
}

module.exports = joinRoomAction